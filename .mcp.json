{
  "mcpServers": {
    "mcp-router": {
      "command": "cmd",
      "args": [
        "/c",
        "npx",
        "-y",
        "@mcp_router/cli@latest",
        "connect"
      ],
      "env": {
        "MCPR_TOKEN": "${MCPR_TOKEN}"
      }
    }
  }
}
