[package]
name = "byebyecode"
version = "1.1.30"
edition = "2021"
description = "byebyecode - Claude Code and Codex compatibility wrapper with statusline and usage tracking"
authors = ["byebyecode Team"]
license = "MIT"
repository = "https://github.com/byebyecode/byebyecode"
readme = "README.md"
keywords = ["claude", "codex", "statusline", "translation", "byebyecode"]
categories = ["command-line-utilities", "development-tools"]

[[bin]]
name = "byebyecode"
path = "src/main.rs"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
clap = { version = "4.0", features = ["derive"] }
toml = "0.8"
toml_edit = "0.22"

ratatui = { version = "0.29", optional = true }
crossterm = { version = "0.28", optional = true }

ansi_term = { version = "0.12", optional = true }
ansi-to-tui = { version = "7.0", optional = true }

ureq = { version = "2.10", features = ["json"], optional = true }
semver = { version = "1.0", optional = true }
chrono = { version = "0.4", features = ["serde"] }
dirs = { version = "5.0", optional = true }
regex = "1.0"
reqwest = { version = "0.11", default-features = false, features = ["json", "blocking", "rustls-tls", "gzip"] }
base64 = "0.21"
which = "6.0"


[features]
default = ["tui", "self-update", "dirs"]
tui = ["ratatui", "crossterm", "ansi_term", "ansi-to-tui"]
self-update = ["ureq", "semver", "dirs"]

[profile.release]
strip = true           # 移除调试符号，能减少约 2-3MB
lto = true            # 链接时优化，减少重复代码
codegen-units = 1     # 更好的优化，略微增加编译时间
opt-level = "z"       # 优化文件大小（或用 "s"）
panic = "abort"       # 移除 panic 展开代码
